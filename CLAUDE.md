# 프로젝트 규칙

이 프로젝트에서 작업할 때 항상 지켜야 하는 규칙입니다.

## 1. 파일 경로 하드코딩 금지
- 코드에 특정 파일 경로를 직접 쓰지 않는다.
- 사용자가 그때그때 업로드/지정하는 파일을 입력으로 받아 처리한다 (예: 커맨드라인 인자, 업로드 경로 변수, 환경변수 등).

## 2. 데이터 파일은 앱 폴더 밖에 보관
- 엑셀(.xlsx, .xls)·CSV 파일은 앱(코드) 폴더 내부에 두지 않는다.
- 데이터 파일 확장자는 `.gitignore`에 등록하여 git 추적에서 제외한다.

## 3. 민감 정보는 .env로 관리
- API 키, 비밀번호, 접속 정보 등은 코드에 직접 넣지 않는다.
- 반드시 `.env` 파일에 저장하고 코드에서는 환경변수로 읽어온다.
- `.env`는 `.gitignore`에 포함한다.

## 4. 응답 언어
- 모든 답변은 항상 한국어로 한다.
